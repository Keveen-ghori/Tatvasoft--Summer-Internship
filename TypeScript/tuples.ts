// Tuple is key-value pairs

let user: [number, string] = [1, 'Kevin'];// its define first key is number and value is string


//accessing value

console.log(user[0]);
user[1].valueOf();
user.push(1); // Drawback of Tuples
console.log(user);